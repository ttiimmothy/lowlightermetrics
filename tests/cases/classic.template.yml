- name: 📗 Classic template - Example
  uses: actionhooks/lowlightermetrics@latest
  with:
    token: MOCKED_TOKEN
    base: header, repositories
    plugin_lines: yes
    use_mocked_data: yes
    verify: yes
